# Keycloak

We have changed to using keycloak in the main docker compose file, instead of the manual command. This realm file was not used, and we manually created a whole new realm as part of the startup process to prevent the re-use of thing like UIDs.
